@model List<FacultyInformationSystem_FIS_.Models.ProfessionalDevelopment>
@using FacultyInformationSystem_FIS_.Models
@{
    Layout = "_PanelLayout";
    ViewData["Title"] = "Professional Development Submissions";
    var currentStatus = ViewBag.StatusFilter as string;
    var currentSearch = ViewBag.SearchFilter as string;
}

<span class="eyebrow">Review Queue</span>
<h2>Professional Development Submissions</h2>

<form method="get" style="display:flex; gap:12px; align-items:end; flex-wrap:wrap; margin:16px 0;">
    <div>
        <label style="display:block; font-size:0.8rem; margin-bottom:4px;">Search</label>
        <input type="text" name="search" value="@currentSearch" placeholder="Faculty, title, or organizer..." />
    </div>
    <div>
        <label style="display:block; font-size:0.8rem; margin-bottom:4px;">Status</label>
        <select name="status">
            <option value="">All</option>
            @foreach (var s in Enum.GetValues(typeof(DocumentStatus)))
            {
                if (currentStatus == s.ToString())
                {
                    <option value="@s" selected>@s</option>
                }
                else
                {
                    <option value="@s">@s</option>
                }
            }
        </select>
    </div>
    <div>
        <button type="submit" class="btn-icon-text">Filter</button>
    </div>
</form>

<div style="display:flex; gap:12px; margin-bottom:20px;">
    <a class="btn-icon-text" href="/professional-development-review/export/excel?status=@currentStatus&amp;search=@currentSearch">Export Excel</a>
    <a class="btn-icon-text" href="/professional-development-review/export/pdf?status=@currentStatus&amp;search=@currentSearch">Export PDF</a>
</div>

<div class="degree-list" style="max-width:900px;">
  @if (!Model.Any())
  {
    <p class="profile-empty">No submissions match your filters.</p>
  }
  @foreach (var activity in Model)
  {
    <div class="degree-card">
      <div>
        <h4>@activity.Title</h4>
        <p class="degree-meta">@activity.User.FullName &middot; @activity.Organizer &middot; @activity.ActivityType</p>
        <p class="degree-notes">Status: @activity.Status</p>
      </div>
      <div class="degree-actions">
        <a href="/professional-development-review/@activity.Id" class="btn-icon-text">Review</a>
      </div>
    </div>
  }
</div>
